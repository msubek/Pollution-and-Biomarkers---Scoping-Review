# 03_figures.R
# Purpose: Generate figures from processed/analyzed data

library(tidyverse)

# --- Load data ---
# data <- read_csv("data/processed/cleaned_data.csv")

# --- Plot ---
# p <- ggplot(data, aes(x = ..., y = ...)) +
#   geom_...()

# --- Save ---
# ggsave("results/figures/figure1.png", p, width = 8, height = 6, dpi = 300)
